# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# NOTE: .terraform.lock.hcl is intentionally committed (do NOT ignore it) so
# provider versions are pinned and reproducible across the team and CI.

# Variable files that may contain secrets
*.tfvars
*.tfvars.json
!example.tfvars

# Plan output
*.tfplan

# CLI config / creds
.terraformrc
terraform.rc

# OS / editor
.DS_Store
.idea/
.vscode/
